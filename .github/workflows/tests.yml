name: Tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  debugger-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Shell syntax check
        run: |
          bash -n re-tools/debug/debug-server.sh
          bash -n re-tools/mcp/setup.sh
      - name: Run debug-server fake-tool test suite
        run: bash re-tools/debug/tests/test-debug-server.sh
      - name: Run MCP setup fake-tool test suite
        run: bash re-tools/mcp/tests/test-mcp-setup.sh
      - name: PowerShell syntax + ownership tests
        run: pwsh -NoProfile -File re-tools/debug/tests/test-windows-ownership.ps1
