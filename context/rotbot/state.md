# RotBot Current State

RotBot provides general repository commands, SignalRot-specific commands, and a
read-only context system. Context identity and state can be listed, displayed,
and included in prompts without exposing local path bindings.
